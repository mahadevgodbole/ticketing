apiVersion: skaffold/v2beta26

kind: Config

deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
    
build:
  # local:
  #   push: false
  googleCloudBuild:
    projectId: ticketing-dev-396308
  artifacts:
    # - image: godbolemahadev/auth
    - image: us.gcr.io/ticketing-dev-396308/auth

    # directory which containe auth image code
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src:  'src/**/*.ts'
            dest: .
